# anomaly-detection-time-series-lstm-autoencoder
LSTM autoencoder for time-series anomaly detection. Trained on normal data to learn temporal patterns, then flags anomalies via reconstruction error. Includes preprocessing, threshold tuning, evaluation (F1), and comparison with Isolation Forest.
